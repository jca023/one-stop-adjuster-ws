import PageShell from '../components/PageShell';

export default function FeaturesPage(): React.JSX.Element {
  return <PageShell title="Features" subtitle="Everything you need to manage flood claims from inspection to submission." />;
}
