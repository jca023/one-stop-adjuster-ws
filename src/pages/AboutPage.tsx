import PageShell from '../components/PageShell';

export default function AboutPage(): React.JSX.Element {
  return <PageShell title="About Us" subtitle="Our mission is to modernize flood adjusting with technology built by adjusters, for adjusters." />;
}
