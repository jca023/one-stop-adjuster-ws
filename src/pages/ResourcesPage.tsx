import PageShell from '../components/PageShell';

export default function ResourcesPage(): React.JSX.Element {
  return <PageShell title="Resources" subtitle="Documentation, training, and support to get the most out of OSA." />;
}
