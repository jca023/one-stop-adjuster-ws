import PageShell from '../components/PageShell';

export default function ClientPortalPage(): React.JSX.Element {
  return <PageShell title="Client Portal" subtitle="Manage your claims, adjusters, and reporting in one place." />;
}
