import PageShell from '../components/PageShell';

export default function ContactPage(): React.JSX.Element {
  return <PageShell title="Contact" subtitle="Get in touch with our team or schedule a demo." />;
}
