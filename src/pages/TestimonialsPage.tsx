import PageShell from '../components/PageShell';

export default function TestimonialsPage(): React.JSX.Element {
  return <PageShell title="Testimonials" subtitle="Hear from adjusters and firms who use One Stop Adjuster every day." />;
}
